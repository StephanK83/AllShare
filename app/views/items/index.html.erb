<head>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />
</head>
<body>
  <div class="container-grid">
    <!--- <div class="checkout-all-our-items">
      <h2>Check out all our items</h2>
      <%= render "shared/search_form", style: "max-width: 400px;" %>
    </div> Search field not neeeded here anymore, but maybe looks better not to have it in the navbar only? --->
    <div class="checkout-all-our-items">
      <h2>See all our items</h2>
    </div>
    <div class="row">
      <% @items.each do |item| %>
        <div class="col-sm-3">
          <div class="card">
            <%= link_to item_path(item) do %>
              <% if item.picture%>
                <%= image_tag item.picture, class: "item-card-img" %>
              <% else %>
                <% item.photos.each do |photo| %>
                  <%= cl_image_tag photo.key, class: "item-card-img" %>
                <% end %>
              <% end %>
              <%= link_to toggle_favorite_item_path(item),
                          class: "favorite-item-link",
                          data: {turbo_method: :post},
                          remote: true do %>
                <% if current_user.favorited?(item) %>
                  <i class="fas fa-heart"></i>
                <% else %>
                  <i class="far fa-heart" aria-hidden="false"></i>
                <% end %>
              <% end %>
            <% end %>
            <div class="card-body">
              <h5 class="card-title"><%= link_to item.name, item_path(item) %></h5>
              <p class="card-text"><%= item.category %></p>
            </div>
          </div>
        </div>
  <% end %>
    </div>
  </div>
    <div class="list-an-item">
      <button type="button" class="btn btn-primary">
        <%= link_to "List an item", new_item_path, class: "list-item-link" %>
      </button>
    </div>
  </div>
</body>
  <style>
    .card {
      position: relative;
    }

    .favorite-item-link {
      position: absolute;
      top: 10px;
      right: 10px;
      z-index: 1;
      color: red;
    }

    .list-an-item {
      display: flex;
      justify-content: center;
    }
  </style>
