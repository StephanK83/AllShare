<div class="container-grid">
  <div class="checkout-all-our-items">
    <h2>Check out all our items</h2>
    <%= render "shared/search_form", style: "max-width: 400px;" %>
  </div>
  <div class="row">
  <% @items.each do |item| %>
    <div class="col-sm-4">
      <div class="card">
        <%= link_to item_path(item) do %>
          <%= image_tag item.picture, class: "card-img-top", style: "height: 200px; object-fit: cover;" %>
        <% end %>
        <div class="card-body">
          <h5 class="card-title"><%= link_to item.name, item_path(item) %></h5>
          <p class="card-text"><%= item.category %></p>
          <%= link_to current_user.favorited?(item) ? "Unlike" : "Like",
            toggle_favorite_item_path(item),
            class: "btn btn-primary",
            data: {turbo_method: :post},
            remote: true %>
        </div>
      </div>
    </div>
  <% end %>
</div>

  <div class="list-an-item">
    <h2>Want to list an item?</h2>
    <button type="button" class="btn btn-primary">
      <%= link_to "List an item", new_item_path, class: "list-item-link" %>
    </button>
  </div>
</div>

<style>
.items-index {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
}

.item-card {
  margin-bottom: 30px;
  border: 1px solid #ccc;
  border-radius: 5px;
  overflow: hidden;
  box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
}

.item-card a {
  display: block;
  position: relative;
}

.item-card img {
  display: block;
  width: 100%;
}

.item-card h2 {
  font-size: 20px;
  margin: 10px 0;
}

.item-card p {
  font-size: 14px;
  color: #666;
  margin-bottom: 10px;
}

.like-button {
  position: absolute;
  top: 10px;
  right: 10px;
}
</style>
