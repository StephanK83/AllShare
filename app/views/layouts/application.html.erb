<!DOCTYPE html>
<html>
  <head>
    <title>AllShare</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_include_tag "overlay" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.css" rel="stylesheet">
    <script src="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.js"></script>
  </head>

  <body>
    <% unless (controller_name == 'sessions' && (action_name == 'new' || action_name == 'create')) || (controller_name == 'registrations' && (action_name == 'new' || action_name == 'create')) %>
      <% if current_page?(root_path) %>
        <!-- Render navbar without search field for home page -->
        <%= render "shared/home_navbar" %>
      <% else %>
        <!-- Render navbar with search field for all other pages -->
        <%= render "shared/navbar" %>
      <% end %>
    <% end %>
    <!--- <%= render "shared/navbar" %> -->
    <%= render "shared/flashes" %>
    <div class="container">
      <%= yield %>
    </div>
    <% unless (controller_name == 'sessions' && (action_name == 'new' || action_name == 'create')) || (controller_name == 'registrations' && (action_name == 'new' || action_name == 'create')) %>
      <footer>
        <%= render "shared/footer" %>
      </footer>
    <% end %>
  </body>
</html>
